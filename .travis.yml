language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-marionette-rails' ]; then cd .. && eval "mv $currentfolder generator-marionette-rails" && cd generator-marionette-rails; fi
